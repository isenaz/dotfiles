# dotfiles for vim

## Pre-requisites

* Homebrew
  * vim-plugのインストールで利用するため
* fzf
  * fzf.vimで利用するため
* the_silver_searcher(agコマンド)
  * fzf.vimで利用するため
  fzf.vimで利用するため
* (Optional) git-delta
  * git-deltaがあると、fzf.vimのgit diffがより美しく表示される

